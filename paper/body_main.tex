\begin{abstract}
Large-scale robot data is becoming a core prerequisite for training embodied foundation models and generalist robot policies. However, robot learning still lacks pretraining-scale datasets that combine broad task coverage, multi-embodiment diversity, standardized data organization, and model-oriented evaluation. In this work, we introduce \textbf{Baihu}, a billion-frame, fully human-teleoperated, multi-platform robot manipulation dataset for embodied foundation model pretraining and evaluation. Baihu contains \textbf{9521.75 hours} of robot data, \textbf{2989 tasks}, \textbf{513575 episodes}, and \textbf{1028349814 frames} across \textbf{13 robot platforms}. The dataset spans fixed-base bimanual manipulators, wheeled mobile manipulators, mobile dual-arm platforms, and humanoid robots, with both gripper-based and dexterous-hand embodiments. All trajectories are collected through human teleoperation on physical robot platforms, and platform-specific HDF5 source records are converted into the LeRobot v2.1 format to support scalable imitation learning and vision-language-action model training.

Beyond dataset construction, Baihu is designed as a reusable data foundation for evaluating embodied model pretraining. We characterize the dataset scale, platform coverage, task composition, human teleoperation protocol, embodiment diversity, and LeRobot v2.1-standardized data organization. To evaluate the utility of Baihu, we conduct offline open-loop zero-shot evaluation with GR00T N1.6 across \textbf{13 platforms} and \textbf{42 paired task-dataset records} drawn from evaluation datasets that are external to Baihu and excluded from training. Compared with a no-training checkpoint, a checkpoint trained on Baihu for one epoch reduces \textbf{Normalized Joint MSE by 99.42\%} and \textbf{Normalized ALL MSE by 96.79\%}. These results suggest that Baihu provides effective large-scale supervision for robot action prediction and can serve as a practical data infrastructure for embodied foundation model development.
\end{abstract}

\section{Introduction}

Recent progress in large language models and vision-language models has shown that model capability can be significantly improved through large-scale pretraining on diverse data. Robotics, however, still lacks the same level of scalable data infrastructure. Robot learning data is expensive to collect, difficult to standardize, and highly dependent on robot embodiment, sensor configuration, task definition, and action representation. As a result, many robot policies are still trained on relatively small task-specific datasets, limiting their generalization across environments, objects, and manipulation skills.

Vision-language-action models and embodied foundation models require large-scale robot interaction data to learn generalizable mappings from visual observations, language instructions, robot states, and actions. Recent efforts such as RoboMIND~\cite{wu2024robomind}, DROID~\cite{khazatsky2024droid}, BridgeData V2~\cite{walke2023bridgedatav2}, and Open X-Embodiment~\cite{oneill2023openx} have demonstrated the importance of large-scale robot manipulation datasets for imitation learning and generalist policy training. These datasets show that diverse real-world robot trajectories, multi-view observations, language annotations, and standardized data protocols can substantially improve robot policy learning. However, there remains a need for larger, human-demonstrated, standardized, pretraining-oriented robot datasets that can support industrial-scale model development and systematic evaluation.

To address this need, we present \textbf{Baihu}, a large-scale, fully human-teleoperated, multi-platform robot manipulation dataset for embodied foundation model pretraining and evaluation. Baihu contains \textbf{9521.75 hours} of robot data, \textbf{2989 tasks}, \textbf{513575 episodes}, and \textbf{1028349814 frames}. Compared with small task-specific datasets, Baihu is designed to support large-scale multi-task policy learning. Compared with unstructured data collections, Baihu emphasizes human teleoperation on physical robot platforms, data management, format standardization, and model-oriented data organization. The dataset is converted from platform-specific HDF5 source records into the LeRobot v2.1 format~\cite{cadene2026lerobot}, making it compatible with modern robot learning pipelines and scalable training workflows.

Beyond platform count, Baihu exhibits substantial diversity in robot morphology, sensing arrangement, end-effector design, and control space. The dataset includes fixed-base bimanual manipulators, wheeled mobile manipulators, mobile dual-arm platforms, and humanoid robots. The represented action spaces range from arm-and-gripper control to richer whole-body configurations involving dexterous hands, head or neck joints, waist motion, legs, and mobile bases. This diversity distinguishes Baihu from datasets collected with a single hardware stack or a limited number of robot embodiments, and makes it a practical testbed for multi-embodiment data standardization and foundation-model pretraining.

The goal of Baihu is not only to accumulate robot data, but also to establish a reusable data foundation for embodied intelligence. A large-scale robot dataset should answer three questions: what skills are represented, how the data is standardized, and how the data can be used to train and evaluate models. Therefore, this paper describes the Baihu dataset from three perspectives: dataset construction, data standardization, and benchmark evaluation. We first introduce the human teleoperation protocol, scale, platform coverage, embodiment diversity, and task composition of Baihu. We then describe the data processing pipeline, including trajectory validation, task organization, action/state schema alignment, and HDF5-to-LeRobot v2.1 format conversion. Finally, we evaluate a GR00T N1.6 checkpoint trained on Baihu through offline open-loop zero-shot evaluation across 13 platforms and 42 paired task-dataset records from datasets that are external to Baihu and excluded from training. Compared with the no-training checkpoint, Baihu training reduces Normalized Joint MSE by \textbf{99.42\%} and Normalized ALL MSE by \textbf{96.79\%}.

The main contributions of this work are as follows:
\begin{enumerate}
  \item We introduce \textbf{Baihu}, a billion-frame, fully human-teleoperated robot manipulation dataset containing 9521.75 hours of data, 2989 tasks, 513575 episodes, and 1028349814 frames across 13 physical robot platforms spanning fixed-base manipulators, wheeled mobile manipulators, mobile dual-arm platforms, and humanoid robots.
  \item We present a standardized robot data organization pipeline that converts platform-specific HDF5 robot operation records into a unified LeRobot v2.1 format, enabling scalable training of robot policies and vision-language-action models.
  \item We characterize the morphology, sensing, end-effector, and control-space diversity of Baihu, including gripper-based and dexterous-hand platforms and action schemas ranging from arm-only manipulation to whole-body control.
  \item We provide an offline zero-shot benchmark across 13 platforms and 42 paired task-dataset records drawn from datasets external to Baihu, showing that a GR00T N1.6 checkpoint trained on Baihu substantially reduces normalized action prediction error compared with a no-training checkpoint.
  \item We provide a structured dataset management framework for large-scale robot data organization, making the dataset suitable for iterative model development.
\end{enumerate}

\section{Related Work}

\subsection{Large-scale robot manipulation datasets}

Large-scale robot manipulation datasets have become a central resource for training generalist robot policies. Recent datasets differ in their collection setting, robot coverage, task diversity, and degree of standardization. RoboMIND focuses on multi-embodiment manipulation data and benchmark evaluation, providing 107k demonstration trajectories across 479 tasks and 96 object classes~\cite{wu2024robomind}. DROID emphasizes in-the-wild real-world robot data, collecting 76k trajectories, 350 hours of data, and manipulation demonstrations across 564 scenes and 84 tasks~\cite{khazatsky2024droid}. BridgeData V2 provides 60,096 trajectories across 24 environments on a publicly available low-cost robot~\cite{walke2023bridgedatav2}. Open X-Embodiment aggregates data from 22 robots across 21 institutions to study cross-embodiment learning at scale~\cite{oneill2023openx}.

Baihu follows the same broad direction of building large-scale robot data for embodied intelligence, but differs in its focus on pretraining-scale fully human-teleoperated dataset construction, structured data management, broad robot morphology and control-space coverage, and HDF5-to-LeRobot v2.1 standardization. Baihu contains 9521.75 hours of robot data, 2989 tasks, 513575 episodes, and 1028349814 frames. This makes Baihu not only a task collection, but a large-scale data foundation for embodied foundation model pretraining.

\begin{table}[t]
\centering
\caption{Comparison with representative large-scale robot manipulation datasets. ``n/r'' indicates that the corresponding statistic is not reported as a primary dataset-level quantity in the cited paper. Task definitions follow the conventions of each source and are therefore not perfectly interchangeable.}
\label{tab:dataset-comparison}
\footnotesize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.08}
\begin{adjustbox}{max width=\textwidth}
\begin{tabular}{lrrrrl}
\toprule
Dataset & Traj. / Ep. & Hours & Platforms & Tasks & Collection \\
\midrule
BridgeData V2~\cite{walke2023bridgedatav2} & 60.1k & n/r & 1 & n/r & Human + scripted \\
DROID~\cite{khazatsky2024droid} & 76k & 350 & 1 & 84 & Human teleoperation \\
RoboMIND~\cite{wu2024robomind} & 107k & n/r & 4 & 479 & Human teleoperation \\
Open X-Embodiment~\cite{oneill2023openx} & $>1$M & n/r & 22 & 160266 & Aggregated datasets \\
\textbf{Baihu} & \textbf{513.6k} & \textbf{9521.75} & \textbf{13} & \textbf{2989} & \textbf{Fully human teleoperation} \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

Table~\ref{tab:dataset-comparison} highlights the combination of properties that distinguishes Baihu. Relative to single-platform datasets such as BridgeData V2 and DROID, Baihu provides substantially broader embodiment coverage. Relative to RoboMIND, Baihu contains more trajectories, tasks, and physical robot platforms. Open X-Embodiment covers more robot embodiments and aggregates a larger overall trajectory corpus, but it is assembled from heterogeneous datasets collected across many institutions. Baihu instead provides a coherently organized corpus of fully human-teleoperated demonstrations across 13 physical platforms, standardized into LeRobot v2.1. The contribution of Baihu therefore lies in the joint combination of scale, human demonstration, embodiment diversity, standardized organization, and external model-oriented evaluation rather than in any single maximum statistic.

\subsection{Multi-embodiment robot learning}

Multi-embodiment learning aims to train policies that can benefit from data collected on different robot platforms. This is challenging because different robots often have different action spaces, joint configurations, camera viewpoints, gripper designs, control frequencies, and task distributions. Open X-Embodiment and RoboMIND both show that cross-robot data can be useful for training more general policies, but they also highlight the need for careful data standardization and evaluation~\cite{oneill2023openx,wu2024robomind}.

Baihu is explicitly multi-embodiment. It covers 13 physical robot platforms spanning fixed-base manipulation, wheeled mobile manipulation, mobile dual-arm operation, and humanoid whole-body control. These platforms use different sensing arrangements and end effectors, including parallel grippers and dexterous hands, and expose heterogeneous action-state schemas. This breadth makes Baihu suitable for studying robot data standardization, multi-platform policy training, and cross-embodiment action prediction.

\subsection{Vision-language-action models}

Vision-language-action models learn to map visual observations, language instructions, robot states, and other context into robot actions. Such models require synchronized observation-action trajectories and task-level annotations. The quality, scale, and diversity of the training data directly affect whether a policy can generalize beyond narrow task-specific demonstrations.

Baihu is designed to support this training paradigm by organizing robot trajectories into standardized observation-action sequences. Its LeRobot v2.1 format enables loading by modern imitation-learning and VLA training pipelines~\cite{cadene2026lerobot}. In this paper, we evaluate this data foundation through offline open-loop zero-shot evaluation using GR00T N1.6 checkpoints on datasets external to Baihu.

\subsection{Data standardization for robot learning}

Robot data is inherently heterogeneous. Different data sources may use different state definitions, action dimensions, camera streams, gripper conventions, control rates, and metadata schemas. Without standardization, it is difficult to train and evaluate a unified robot policy across multiple robots and tasks.

Baihu addresses this challenge through structured dataset management and HDF5-to-LeRobot v2.1 standardization. Structured data management makes it possible to track data additions, data cleaning, normalization changes, and embodiment-specific fixes. The LeRobot v2.1 format further provides a training-oriented data interface for scalable loading, sharing, and evaluation. This is especially important for large-scale robot pretraining datasets, where small inconsistencies in state/action mapping can affect many downstream training runs.

\subsection{Positioning of Baihu}

Compared with prior datasets, Baihu is positioned as a billion-frame, fully human-teleoperated, multi-platform robot manipulation dataset for embodied foundation model pretraining and evaluation. Its key distinction is not only its scale, but the combination of human teleoperation on physical robot platforms, broad morphology and control-space diversity, platform-specific schema preservation, HDF5-to-LeRobot v2.1 standardization, and direct offline evaluation with a foundation-model checkpoint on datasets external to Baihu. Baihu is therefore particularly complementary to datasets built around a single hardware stack or a small number of embodiments, while targeting the specific need for large-scale multi-embodiment pretraining infrastructure.

\section{Baihu Dataset}

\subsection{Dataset scale}

Baihu provides a large-scale robot data foundation for embodied model pretraining. It contains \textbf{9521.75 hours} of robot manipulation data, \textbf{2989 tasks}, \textbf{513575 episodes}, and \textbf{1028349814 frames} across \textbf{13 robot platforms}.

This scale makes Baihu suitable for large-scale pretraining of robot policies, rather than only task-specific imitation learning. Based on the total frame count and total duration, Baihu has an average recording rate of approximately 30 frames per second. Each episode contains approximately 2002 frames on average, corresponding to about 66.7 seconds per episode.